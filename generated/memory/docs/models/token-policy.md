# TokenPolicy

## Example Usage

```typescript
import { TokenPolicy } from "@compose-market/sdk/models";

let value: TokenPolicy = "returns compact prompt only";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"returns compact prompt only" | "returns metadata only" | Unrecognized<string>
```