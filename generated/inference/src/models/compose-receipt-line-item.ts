/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type ComposeReceiptLineItem = {
  key: string;
  unit: string;
  quantity: number;
  unitPriceUsd?: number | undefined;
  /**
   * Non-negative integer amount in USDC atomic units.
   */
  amountWei: string;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const ComposeReceiptLineItem$inboundSchema: z.ZodMiniType<
  ComposeReceiptLineItem,
  unknown
> = z.pipe(
  z.catchall(
    z.object({
      key: types.string(),
      unit: types.string(),
      quantity: types.number(),
      unit_price_usd: types.optional(types.number()),
      amount_wei: types.string(),
    }),
    z.any(),
  ),
  z.transform((v) => {
    return remap$(v, {
      "unit_price_usd": "unitPriceUsd",
      "amount_wei": "amountWei",
    });
  }),
);

export function composeReceiptLineItemFromJSON(
  jsonString: string,
): SafeParseResult<ComposeReceiptLineItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ComposeReceiptLineItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ComposeReceiptLineItem' from JSON`,
  );
}
