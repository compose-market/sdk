/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod/v4-mini";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { pathToFunc } from "../lib/url.js";
import * as operations from "../models/operations/index.js";
import { WorkflowStreamCreateServerComposeApi, WorkflowStreamCreateServerList, } from "../models/operations/workflow-stream-create.js";
import { APIPromise } from "../types/async.js";
export function workflowsStreamCreate(client, request, options) {
    return new APIPromise($do(client, request, options));
}
async function $do(client, request, options) {
    const parsed = safeParse(request, (value) => z.parse(operations.WorkflowStreamCreateRequest$outboundSchema, value), "Input validation failed");
    if (!parsed.ok) {
        return [parsed, { status: "invalid" }];
    }
    const payload = parsed.value;
    const body = encodeJSON("body", payload.body, { explode: true });
    const baseURL = options?.serverURL
        || pathToFunc(WorkflowStreamCreateServerList[WorkflowStreamCreateServerComposeApi], { charEncoding: "percent" })();
    const pathParams = {
        walletAddress: encodeSimple("walletAddress", payload.walletAddress, {
            explode: false,
            charEncoding: "percent",
        }),
    };
    const path = pathToFunc("/workflow/{walletAddress}/stream")(pathParams);
    const headers = new Headers(compactMap({
        "Content-Type": "application/json",
        Accept: "text/event-stream",
        "x-session-active": encodeSimple("x-session-active", payload["x-session-active"], { explode: false, charEncoding: "none" }),
        "x-session-budget-remaining": encodeSimple("x-session-budget-remaining", payload["x-session-budget-remaining"], { explode: false, charEncoding: "none" }),
        "x-session-user-address": encodeSimple("x-session-user-address", payload["x-session-user-address"], { explode: false, charEncoding: "none" }),
    }));
    const context = {
        options: client._options,
        baseURL: baseURL ?? "",
        operationID: "workflow_stream_create",
        oAuth2Scopes: null,
        resolvedSecurity: null,
        securitySource: null,
        retryConfig: options?.retries
            || client._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
    };
    const requestRes = client._createRequest(context, {
        method: "POST",
        baseURL: baseURL,
        path: path,
        headers: headers,
        body: body,
        userAgent: client._options.userAgent,
        timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req = requestRes.value;
    const doResult = await client._do(req, {
        context,
        isErrorStatusCode: (statusCode) => matchStatusCode({ status: statusCode }, ["4XX", "5XX"]),
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req }];
    }
    const response = doResult.value;
    const responseFields = {
        HttpMeta: { Response: response, Request: req },
    };
    const [result] = await M.match(M.text(200, operations.WorkflowStreamCreateResponse$inboundSchema, {
        ctype: "text/event-stream",
        hdrs: true,
        key: "Result",
    }), M.fail("4XX"), M.fail("5XX"))(response, req, { extraFields: responseFields });
    if (!result.ok) {
        return [result, { status: "complete", request: req, response }];
    }
    return [result, { status: "complete", request: req, response }];
}
//# sourceMappingURL=workflows-stream-create.js.map